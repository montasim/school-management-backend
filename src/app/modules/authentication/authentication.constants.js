/**
 * @constant
 * @namespace ID_CONSTANTS
 * @description Constants related to ID generation and validation.
 * @property {number} MIN_LENGTH - Minimum allowed length for password.
 * @property {number} MAX_LENGTH - Maximum allowed length for password.
 */
export const ID_CONSTANTS = {
    MIN_LENGTH: 8,
    MAX_LENGTH: 20
};
