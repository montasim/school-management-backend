/**
 * @fileoverview Constants for Website Banner Validation.
 *
 * This file defines a set of constants used throughout the application, specifically for
 * validating Website Banner data. These constants include various parameters like ID prefixes,
 * minimum and maximum lengths for different form fields, such as the Website Banner.
 * Defining these constants in a separate file ensures consistency and ease of
 * maintenance, as they can be easily updated and reused across different parts of the application.
 */

const WEBSITE_BANNER_ID_PREFIX = 'websiteBanner';
const WEBSITE_BANNER_ID_MIN_LENGTH = 23;
const WEBSITE_BANNER_ID_MAX_LENGTH = 23;

export const WEBSITE_BANNER_CONSTANTS = {
    WEBSITE_BANNER_ID_PREFIX,
    WEBSITE_BANNER_ID_MIN_LENGTH,
    WEBSITE_BANNER_ID_MAX_LENGTH,
};