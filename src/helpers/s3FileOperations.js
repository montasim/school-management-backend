const uploadFile = async (file) => {
    // Add your S3 upload logic here
};

const deleteFile = async (fileId) => {
    // Add your S3 delete logic here
};

const s3FileOperations = {
    uploadFile,
    deleteFile,
};

export default s3FileOperations;